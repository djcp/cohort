<%= error_messages_for :object => @object %>
<fieldset class="form <%= @object.class.name.to_s.downcase %>">
<legend><%= @object.new_record? ? "Add " : "Edit " %> <%= @object.class.name.to_s %></legend>

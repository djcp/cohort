<%= error_messages_for :object => @object %>
<fieldset class="form <%= @object.class.name.to_s.downcase %>">
  <legend><%= @object.new_record? ? "Add #{@object.class.name.to_s}" : "Edit #{@object.class.name.to_s} \"#{h @object.name_for_display}\"" %></legend>

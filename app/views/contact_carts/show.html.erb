<p><b>Title:</b><%= h @contact_cart.title || ' -- None Given -- ' %></p>

<p><b>User:</b><%= h @contact_cart.user.username %></p>
<p><b>Global:</b><%= h @contact_cart.global %></p>

<p><b>Contacts:</b><%= render :partial => "contacts", :locals => {:cart => @contact_cart} %></p>

<%= link_to 'Make Active', contact_cart_make_active_path(@contact_cart) %>
<% if @session_user == @contact_cart.user %>
  | <%= link_to 'Edit', edit_contact_cart_path(@contact_cart) %>
<% end %>
| <%= link_to 'Back', contact_carts_path %>

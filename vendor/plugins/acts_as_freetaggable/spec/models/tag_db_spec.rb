require File.join(File.dirname(__FILE__), "/../spec_helper")

describe Tag do
  context "db triggers" do
    it "should still delete children when deleted" do
      pending "db triggers need to get created"
    end

  end
end

